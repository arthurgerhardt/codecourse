"use strict";
// Função que soma dois números
function sums(a, b) {
    return a + b;
}
// Chamando a função de soma e armazenando o resultado
var resultadoSoma = sums(5, 10);
// Exibindo o resultado no console
console.log("O resultado \u00E9: ".concat(resultadoSoma));
