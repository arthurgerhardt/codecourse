// Função que soma dois números
function sums(a: number, b: number): number {
  return a + b;
}

// Chamando a função de soma e armazenando o resultado
let resultadoSoma = sums(5, 10);

// Exibindo o resultado no console
console.log(`O resultado é: ${resultadoSoma}`);
